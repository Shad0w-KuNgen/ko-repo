# ko-repo-dotnet

Simple setup notes:

1. Run `npm install` first to install dependencies.
2. Then run `npm start` to start the app.
3. Open the project only after these commands complete.

If `npm install` is skipped, `npm start` may fail.